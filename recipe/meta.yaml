{% set version = "2.0.1" %}

package:
  name: astropy
  version: {{version}}

source:
  fn: astropy-{{version}}.tar.gz
  url: https://pypi.io/packages/source/a/astropy/astropy-{{version}}.tar.gz
  md5: f7bc6ff800575705d8ad0214a89ba489
  patches:
    # invalid compressed data trips up some virus scanners.  This patch can be removed in astropy 2.0.2
    - create_invalid_compressed_data_on_fly.patch


build:
  detect_binary_files_with_prefix: False
  entry_points:
    - fits2bitmap = astropy.visualization.scripts.fits2bitmap:main
    - fitscheck = astropy.io.fits.scripts.fitscheck:main
    - fitsdiff = astropy.io.fits.scripts.fitsdiff:main
    - fitsheader = astropy.io.fits.scripts.fitsheader:main
    - fitsinfo = astropy.io.fits.scripts.fitsinfo:main
    - samp_hub = astropy.samp.hub_script:hub_script
    - volint = astropy.io.votable.volint:main
    - wcslint = astropy.wcs.wcslint:main
  number: 2

requirements:

  build:
    - python
    - setuptools
    - numpy x.x

  run:
    - python
    - numpy x.x
    - pytest

test:
  commands:
    - fits2bitmap --help
    - fitscheck --help
    - fitsdiff --help
    - fitsheader --help
    - fitsinfo --help
    - samp_hub --help
    - volint --help
    - wcslint --help
  imports:
    - astropy

about:
  home: http://www.astropy.org/
  license: BSD
  summary: Community-developed Python Library for Astronomy
  description: |
    The Astropy Project is a community effort to develop a single package for
    Astronomy in Python. It contains core functionality and common tools
    needed for performing astronomy and astrophysics research with Python.
  doc_url: http://docs.astropy.org/en/stable/
  dev_url: https://github.com/astropy/astropy

extra:
  recipe-maintainers:
    - astrofrog
    - mwcraig
    - bsipocz
